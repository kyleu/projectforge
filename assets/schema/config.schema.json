{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://projectforge.dev/schemas/config.schema.json",
  "title": "Config",
  "type": "object",
  "properties": {
    "grpcClass": {
      "description": "when set, overrides the gRPC class for generated code",
      "type": "string"
    },
    "grpcPackage": {
      "description": "when set, overrides the gRPC package for generated code",
      "type": "string"
    }
  }
}
