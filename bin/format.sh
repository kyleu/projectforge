#!/bin/bash
# Code generated by Project Forge, see https://projectforge.dev for details.

## Formatting code from all projects

set -euo pipefail
dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $dir/..

echo "=== formatting ==="
gofumpt -w $(find . -type f -name "*.go" | grep -v \\./module | grep -v \\./testproject | grep -v \\./tmp | grep -v .html.go | grep -v .sql.go)
